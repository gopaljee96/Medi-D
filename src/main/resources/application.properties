spring.datasource.url=jdbc:postgresql://localhost:5432/mydb
spring.datasource.username=medico
spring.datasource.password=medico123
spring.datasource.driver-class-name=org.postgresql.Driver
# Disable Hibernate automatic schema updates here. Use migrations or run ALTERs as DB owner.
# Options: 'validate' to check mappings (no DDL), or 'none' to completely disable.
spring.jpa.hibernate.ddl-auto=validate
# JWT Configuration - Use minimum 512 bits (64 chars) for HS512
jwt.secret=your-super-secret-key-that-is-at-least-512-bits-long-for-hs512-algorithm-production-secure
